# LLM / local OpenAI-compatible model
LLM_BASE_URL=http://localhost:8000/v1
LLM_API_KEY=local-llm-token-or-placeholder
LLM_MODEL_NAME=llama-3.1-8b-instruct

# Git / MCP
GIT_REPO_SVC_ACCOUNTING=your-org/svc-accounting
GIT_REPO_TESTWEAVER=your-org/testweaver
GIT_MCP_ENDPOINT=http://localhost:9000/git-mcp   # or stdio if using MCP spec
GIT_TOKEN=your-git-token

# Vector store (can start with local Chroma or simple JSON)
VECTOR_DB_PATH=./data/vector_store
DOC_STORE_PATH=./data/docs

# API
TESTWEAVER_HOST=0.0.0.0
TESTWEAVER_PORT=9090
